# Backend

## Start Backend

```bash
npm run dev
```

## Open Database

```bash
npx prisma studio
```

## Troubleshooting Steps:

- Use Prisma Studio (`npx prisma studio`) to inspect the database.
- Run `npx prisma db push` to synchronize the database schema with the Prisma schema.
