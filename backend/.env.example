# ============================================
# Development Environment Configuration
# ============================================
# Copy this file to .env and fill in your values

# ============================================
# API Tokens (Optional - only if using sync)
# ============================================
# Get Toggl token from: https://track.toggl.com/profile
TOGGL_API_TOKEN="your_toggl_api_token_here"

# Get Tempo token from: Jira > Apps > Tempo > Settings > API Integration
TEMPO_API_TOKEN="your_tempo_api_token_here"

# Optional: Jira base URL for issue links
# JIRA_BASE_URL="https://your-company.atlassian.net"

# ============================================
# Authentication Secrets
# ============================================
# Generate with: openssl rand -hex 32 (must be 32 bytes = 64 hex chars)
JWT_SECRET=your_jwt_secret_here_64_characters_minimum_required_value
SESSION_SECRET=your_session_secret_here_64_characters_minimum_required

# Generate with: npx bcrypt-cli hash "your_password"
# Or: node -e "console.log(require('bcrypt').hashSync('yourpassword', 12))"
ADMIN_PASSWORD_HASH="$2b$12$your_bcrypt_hash_here"

# Admin username
ADMIN_USER=admin

# ============================================
# Server Configuration
# ============================================
# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Backend server port
PORT=3000

# ============================================
# Database Configuration
# ============================================
# Development: SQLite (default, no config needed)
# Docker development: PostgreSQL
DATABASE_URL="postgresql://timetracker:devpassword@localhost:5432/timetracker"

# Production: Uses Docker Secrets (see README.md)
